# Game logic (to build)

The strike/steal state machine and scoring, kept separate from views so it can be unit-tested by the tester agent. Implement exactly to docs/GAME_RULES.md. Never trust the client; recompute scores server-side.
